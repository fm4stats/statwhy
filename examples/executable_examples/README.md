# How to execute the examples in this directory

```bash
$ dune build
$ dune exec ./example1.exe
$ dune exec ./example2.exe
...
```

To execute these examples, you need to install
1. Pyml (ocaml package)
2. scipy (python library)
